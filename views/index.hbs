<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Index</title>
</head>
<body>

  <h1>Handlebars</h1>
  {{#each users as |user|}}
  <div>
    <img src="{{user.username}}_sm.jpg" alt="">
    <a href="/{{user.username}}">{{user.name.full}}</a>
    {{/each}}
  </div>

</body>
</html>